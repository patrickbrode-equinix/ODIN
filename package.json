{
    "name": "odin-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "install:all": "npm install && npm install --prefix Backend && npm install --prefix Frontend && npm install --prefix teams-bot/odin",
        "dev": "concurrently -k -n \"BACKEND,FRONTEND,BOT\" -c \"bgBlue.bold,bgMagenta.bold,bgGreen.bold\" \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:bot\"",
        "dev:frontend": "npm run dev --prefix Frontend",
        "dev:backend": "npm run dev --prefix Backend",
        "dev:bot": "npm run dev --prefix teams-bot/odin"
    },
    "devDependencies": {
        "concurrently": "^9.1.0"
    }
}